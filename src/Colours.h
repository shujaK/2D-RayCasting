#pragma once
#include <SFML/Graphics.hpp>

// global color definitions for the application
namespace Colours
{
    inline sf::Color colour1(38, 39, 48, 255);   // dark background
    inline sf::Color colour2(239, 240, 209, 255); // light wall/ray color
    inline sf::Color colour3(211, 63, 73, 255);   // red accent (unused)
}
